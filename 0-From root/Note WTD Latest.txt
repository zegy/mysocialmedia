small all "base url" links
gallery's data-title (both post detail's page and it's script - update)
js's text area is rn using val(), change them to text or else. Because value of textarea is what inside the tag, not the "value" attr